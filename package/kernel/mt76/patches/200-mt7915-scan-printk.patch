Index: mt76-2025.11.06~eb567bc7/mac80211.c
===================================================================
--- mt76-2025.11.06~eb567bc7.orig/mac80211.c
+++ mt76-2025.11.06~eb567bc7/mac80211.c
@@ -1021,9 +1021,21 @@ int __mt76_set_channel(struct mt76_phy *
 		       bool offchannel)
 {
 	struct mt76_dev *dev = phy->dev;
+	bool prev_offchannel = phy->offchannel;
+	bool same_chandef = cfg80211_chandef_identical(&phy->chandef, chandef);
+	u32 prev_main_freq = phy->main_chandef.chan ?
+			     phy->main_chandef.chan->center_freq : 0;
+	u32 new_freq = chandef->chan ? chandef->chan->center_freq : 0;
 	int timeout = HZ / 5;
 	int ret;
 
+	/* Avoid redundant "re-apply home channel" calls before going offchannel.
+	 * mac80211/mt76 may call set_channel() even if chandef didn't change.
+	 * For scan blackout analysis we want only real channel moves.
+	 */
+	if (!prev_offchannel && !offchannel && same_chandef)
+		return 0;
+
 	set_bit(MT76_RESET, &phy->state);
 
 	mt76_worker_disable(&dev->tx_worker);
@@ -1046,6 +1058,40 @@ int __mt76_set_channel(struct mt76_phy *
 
 	ret = dev->drv->set_channel(phy);
 
+	if (!ret) {
+		bool dbg_scan = test_bit(MT76_SCANNING, &phy->state) ||
+				test_bit(MT76_HW_SCANNING, &phy->state);
+		bool dbg_roc = !!phy->roc_vif;
+
+		if ((dbg_scan || dbg_roc) && !prev_offchannel && offchannel) {
+			u64 now_ns = ktime_get_boottime_ns();
+
+			phy->dbg_offchan_enter_ns = now_ns;
+			phy->dbg_offchan_enter_freq = new_freq;
+			phy->dbg_offchan_enter_main_freq = prev_main_freq;
+			printk(KERN_INFO
+			       "mt76: event=offchan_enter ts_ms=%llu chip=0x%04x band=%u freq=%u main_freq=%u scan=%d roc=%d\n",
+			       (unsigned long long)div_u64(now_ns, NSEC_PER_MSEC),
+			       mt76_chip(dev), phy->band_idx, new_freq, prev_main_freq,
+			       dbg_scan, dbg_roc);
+		} else if (prev_offchannel && !offchannel && phy->dbg_offchan_enter_ns) {
+			u64 now_ns = ktime_get_boottime_ns();
+			u64 dur_ms = div_u64(now_ns - phy->dbg_offchan_enter_ns,
+					     NSEC_PER_MSEC);
+
+			printk(KERN_INFO
+			       "mt76: event=offchan_exit ts_ms=%llu chip=0x%04x band=%u freq=%u main_freq=%u offchan_freq=%u offchan_dur_ms=%llu scan=%d roc=%d\n",
+			       (unsigned long long)div_u64(now_ns, NSEC_PER_MSEC),
+			       mt76_chip(dev),
+			       phy->band_idx, new_freq,
+			       phy->main_chandef.chan ? phy->main_chandef.chan->center_freq : 0,
+			       phy->dbg_offchan_enter_freq,
+			       (unsigned long long)dur_ms,
+			       dbg_scan, dbg_roc);
+			phy->dbg_offchan_enter_ns = 0;
+		}
+	}
+
 	clear_bit(MT76_RESET, &phy->state);
 	mt76_worker_enable(&dev->tx_worker);
 	mt76_worker_schedule(&dev->tx_worker);
@@ -1923,16 +1969,49 @@ void mt76_sw_scan(struct ieee80211_hw *h
 		  const u8 *mac)
 {
 	struct mt76_phy *phy = hw->priv;
+	struct mt76_dev *dev = phy->dev;
+	u64 now_ns;
+	u32 freq = 0;
 
 	set_bit(MT76_SCANNING, &phy->state);
+	phy->dbg_scan_probe_reqs = 0;
+
+	now_ns = ktime_get_boottime_ns();
+	if (phy->chandef.chan)
+		freq = phy->chandef.chan->center_freq;
+	phy->dbg_scan_start_ns = now_ns;
+	phy->dbg_scan_start_freq = freq;
+
+	printk(KERN_INFO
+	       "mt76: event=scan_sw_start ts_ms=%llu chip=0x%04x band=%u freq=%u\n",
+	       (unsigned long long)div_u64(now_ns, NSEC_PER_MSEC),
+	       mt76_chip(dev), phy->band_idx, freq);
 }
 EXPORT_SYMBOL_GPL(mt76_sw_scan);
 
 void mt76_sw_scan_complete(struct ieee80211_hw *hw, struct ieee80211_vif *vif)
 {
 	struct mt76_phy *phy = hw->priv;
+	struct mt76_dev *dev = phy->dev;
+	u64 now_ns, dur_ms = 0;
+	u32 freq = 0;
 
 	clear_bit(MT76_SCANNING, &phy->state);
+
+	now_ns = ktime_get_boottime_ns();
+	if (phy->chandef.chan)
+		freq = phy->chandef.chan->center_freq;
+	if (phy->dbg_scan_start_ns)
+		dur_ms = div_u64(now_ns - phy->dbg_scan_start_ns,
+				 NSEC_PER_MSEC);
+
+	printk(KERN_INFO
+	       "mt76: event=scan_sw_done ts_ms=%llu chip=0x%04x band=%u freq=%u dur_ms=%llu probe_reqs=%u\n",
+	       (unsigned long long)div_u64(now_ns, NSEC_PER_MSEC),
+	       mt76_chip(dev), phy->band_idx, freq,
+	       (unsigned long long)dur_ms, phy->dbg_scan_probe_reqs);
+
+	phy->dbg_scan_start_ns = 0;
 }
 EXPORT_SYMBOL_GPL(mt76_sw_scan_complete);
 
Index: mt76-2025.11.06~eb567bc7/mt76.h
===================================================================
--- mt76-2025.11.06~eb567bc7.orig/mt76.h
+++ mt76-2025.11.06~eb567bc7/mt76.h
@@ -876,6 +876,14 @@ struct mt76_phy {
 	bool offchannel;
 	bool radar_enabled;
 
+	/* Debug-only state */
+	u64 dbg_offchan_enter_ns;
+	u32 dbg_offchan_enter_freq;
+	u32 dbg_offchan_enter_main_freq;
+	u64 dbg_scan_start_ns;
+	u32 dbg_scan_start_freq;
+	u32 dbg_scan_probe_reqs;
+
 	struct delayed_work roc_work;
 	struct ieee80211_vif *roc_vif;
 	struct mt76_vif_link *roc_link;
@@ -1021,6 +1029,11 @@ struct mt76_dev {
 		struct mt76_vif_link *mlink;
 		struct mt76_phy *phy;
 		int chan_idx;
+
+		/* Debug timing for scan start/end */
+		u64 start_boottime_ns;
+		u32 start_main_freq;
+		u32 start_first_freq;
 	} scan;
 
 #ifdef CONFIG_NL80211_TESTMODE
Index: mt76-2025.11.06~eb567bc7/scan.c
===================================================================
--- mt76-2025.11.06~eb567bc7.orig/scan.c
+++ mt76-2025.11.06~eb567bc7/scan.c
@@ -10,15 +10,33 @@ static void mt76_scan_complete(struct mt
 	struct cfg80211_scan_info info = {
 		.aborted = abort,
 	};
+	u64 now_ns, now_ms, dur_ms = 0;
+	u32 last_freq = 0, main_freq = 0;
 
 	if (!phy)
 		return;
 
+	now_ns = ktime_get_boottime_ns();
+	now_ms = div_u64(now_ns, NSEC_PER_MSEC);
+	if (dev->scan.start_boottime_ns)
+		dur_ms = div_u64(now_ns - dev->scan.start_boottime_ns,
+				 NSEC_PER_MSEC);
+	if (dev->scan.chan)
+		last_freq = dev->scan.chan->center_freq;
+	if (phy->main_chandef.chan)
+		main_freq = phy->main_chandef.chan->center_freq;
+
 	clear_bit(MT76_SCANNING, &phy->state);
 
 	if (dev->scan.chan && phy->main_chandef.chan &&
 	    !test_bit(MT76_MCU_RESET, &dev->phy.state))
 		mt76_set_channel(phy, &phy->main_chandef, false);
+
+	printk(KERN_INFO
+	       "mt76: event=scan_hw_done ts_ms=%llu chip=0x%04x band=%u main_freq=%u last_freq=%u dur_ms=%llu aborted=%d\n",
+	       (unsigned long long)now_ms, mt76_chip(dev), phy->band_idx,
+	       main_freq, last_freq, (unsigned long long)dur_ms, abort);
+
 	mt76_put_vif_phy_link(phy, dev->scan.vif, dev->scan.mlink);
 	memset(&dev->scan, 0, sizeof(dev->scan));
 	if (!test_bit(MT76_MCU_RESET, &dev->phy.state))
@@ -86,7 +104,7 @@ void mt76_scan_work(struct work_struct *
 	struct cfg80211_scan_request *req = dev->scan.req;
 	struct cfg80211_chan_def chandef = {};
 	struct mt76_phy *phy = dev->scan.phy;
-	int duration = HZ / 9; /* ~110 ms */
+	int duration = HZ / 30; /* ~33 ms */
 	int i;
 
 	if (dev->scan.chan_idx >= req->n_channels) {
@@ -108,7 +126,7 @@ void mt76_scan_work(struct work_struct *
 	    chandef.chan->flags & (IEEE80211_CHAN_NO_IR | IEEE80211_CHAN_RADAR))
 		goto out;
 
-	duration = HZ / 16; /* ~60 ms */
+	duration = HZ / 50; /* ~20 ms */
 	local_bh_disable();
 	for (i = 0; i < req->n_ssids; i++)
 		mt76_scan_send_probe(dev, &req->ssids[i]);
@@ -130,6 +148,8 @@ int mt76_hw_scan(struct ieee80211_hw *hw
 	struct mt76_dev *dev = phy->dev;
 	struct mt76_vif_link *mlink;
 	int ret = 0;
+	u64 now_ns, now_ms;
+	u32 main_freq = 0, first_freq = 0;
 
 	if (hw->wiphy->n_radio > 1) {
 		phy = dev->band_phys[req->req.channels[0]->band];
@@ -151,11 +171,27 @@ int mt76_hw_scan(struct ieee80211_hw *hw
 		goto out;
 	}
 
+	now_ns = ktime_get_boottime_ns();
+	now_ms = div_u64(now_ns, NSEC_PER_MSEC);
+	if (phy->main_chandef.chan)
+		main_freq = phy->main_chandef.chan->center_freq;
+	if (req->req.n_channels)
+		first_freq = req->req.channels[0]->center_freq;
+
 	memset(&dev->scan, 0, sizeof(dev->scan));
 	dev->scan.req = &req->req;
 	dev->scan.vif = vif;
 	dev->scan.phy = phy;
 	dev->scan.mlink = mlink;
+	dev->scan.start_boottime_ns = now_ns;
+	dev->scan.start_main_freq = main_freq;
+	dev->scan.start_first_freq = first_freq;
+
+	printk(KERN_INFO
+	       "mt76: event=scan_hw_start ts_ms=%llu chip=0x%04x band=%u main_freq=%u first_freq=%u n_channels=%u req_dur_ms=%u\n",
+	       (unsigned long long)now_ms, mt76_chip(dev), phy->band_idx,
+	       main_freq, first_freq, req->req.n_channels, req->req.duration);
+
 	ieee80211_queue_delayed_work(dev->phy.hw, &dev->scan_work, 0);
 
 out:
Index: mt76-2025.11.06~eb567bc7/mt7915/main.c
===================================================================
--- mt76-2025.11.06~eb567bc7.orig/mt7915/main.c
+++ mt76-2025.11.06~eb567bc7/mt7915/main.c
@@ -324,9 +324,52 @@ int mt7915_set_channel(struct mt76_phy *
 {
 	struct mt7915_phy *phy = mphy->priv;
 	struct mt7915_dev *dev = phy->dev;
+	struct cfg80211_chan_def *chandef = &phy->mt76->chandef;
+	u64 now_ns = ktime_get_boottime_ns();
+	u32 freq = chandef->chan ? chandef->chan->center_freq : 0;
+	bool offchan = phy->mt76->offchannel;
+	bool same_prog = phy->dbg_prog_valid &&
+			 phy->dbg_prog_offchan == offchan &&
+			 phy->dbg_prog_freq == freq &&
+			 phy->dbg_prog_width == chandef->width &&
+			 phy->dbg_prog_center1 == chandef->center_freq1 &&
+			 phy->dbg_prog_center2 == chandef->center_freq2;
+	bool scan_offchan = offchan &&
+			    (test_bit(MT76_SCANNING, &phy->mt76->state) ||
+			     test_bit(MT76_HW_SCANNING, &phy->mt76->state));
 	int ret;
 
-	if (dev->cal) {
+	/* Track offchannel enter/exit at the driver level for mt791x. */
+	if (!phy->dbg_last_offchannel && offchan) {
+		phy->dbg_offchan_enter_ns = now_ns;
+		phy->dbg_offchan_enter_freq = freq;
+		printk(KERN_INFO "mt76: event=offchan_enter_drv ts_ms=%llu chip=0x%04x band=%u freq=%u\n",
+		       (unsigned long long)div_u64(now_ns, NSEC_PER_MSEC),
+		       mt76_chip(&dev->mt76), phy->mt76->band_idx, freq);
+	} else if (phy->dbg_last_offchannel && !offchan && phy->dbg_offchan_enter_ns) {
+		u64 dur_ms = div_u64(now_ns - phy->dbg_offchan_enter_ns,
+				     NSEC_PER_MSEC);
+
+		printk(KERN_INFO "mt76: event=offchan_exit_drv ts_ms=%llu chip=0x%04x band=%u freq=%u offchan_freq=%u offchan_dur_ms=%llu\n",
+		       (unsigned long long)div_u64(now_ns, NSEC_PER_MSEC),
+		       mt76_chip(&dev->mt76), phy->mt76->band_idx, freq,
+		       phy->dbg_offchan_enter_freq, (unsigned long long)dur_ms);
+		phy->dbg_offchan_enter_ns = 0;
+	}
+	phy->dbg_last_offchannel = offchan;
+
+	/* Avoid reprogramming firmware if mac80211 calls ->set_channel() with an
+	 * identical chandef/offchannel state (common at scan start).
+	 */
+	if (same_prog) {
+		ret = 0;
+		goto out;
+	}
+
+	/* During offchannel scan, avoid extra per-channel calibration work.
+	 * Home-channel programming will run the full sequence.
+	 */
+	if (dev->cal && !scan_offchan) {
 		ret = mt7915_mcu_apply_tx_dpd(phy);
 		if (ret)
 			goto out;
@@ -336,12 +379,22 @@ int mt7915_set_channel(struct mt76_phy *
 	if (ret)
 		goto out;
 
+	phy->dbg_prog_valid = true;
+	phy->dbg_prog_offchan = offchan;
+	phy->dbg_prog_freq = freq;
+	phy->dbg_prog_width = chandef->width;
+	phy->dbg_prog_center1 = chandef->center_freq1;
+	phy->dbg_prog_center2 = chandef->center_freq2;
+
 	mt7915_mac_set_timing(phy);
-	ret = mt7915_dfs_init_radar_detector(phy);
-	mt7915_mac_cca_stats_reset(phy);
 
-	mt7915_mac_reset_counters(phy);
-	phy->noise = 0;
+	if (!scan_offchan) {
+		ret = mt7915_dfs_init_radar_detector(phy);
+		mt7915_mac_cca_stats_reset(phy);
+
+		mt7915_mac_reset_counters(phy);
+		phy->noise = 0;
+	}
 
 out:
 	if (!mt76_testmode_enabled(phy->mt76))
@@ -895,6 +948,7 @@ static void mt7915_tx(struct ieee80211_h
 	struct ieee80211_tx_info *info = IEEE80211_SKB_CB(skb);
 	struct ieee80211_vif *vif = info->control.vif;
 	struct mt76_wcid *wcid = &dev->mt76.global_wcid;
+	struct ieee80211_hdr *hdr;
 
 	if (control->sta) {
 		struct mt7915_sta *sta;
@@ -910,6 +964,22 @@ static void mt7915_tx(struct ieee80211_h
 		wcid = &mvif->sta.wcid;
 	}
 
+	/* Helps to distinguish active scan (probe requests are sent) vs passive scan. */
+	if (test_bit(MT76_SCANNING, &mphy->state) &&
+	    skb_headlen(skb) >= sizeof(*hdr)) {
+		hdr = (struct ieee80211_hdr *)skb->data;
+		if (ieee80211_is_probe_req(hdr->frame_control)) {
+			mphy->dbg_scan_probe_reqs++;
+			printk_ratelimited(KERN_INFO
+					   "mt76: event=scan_probe_req_tx ts_ms=%llu chip=0x%04x band=%u freq=%u probe_reqs=%u\n",
+					   (unsigned long long)div_u64(ktime_get_boottime_ns(),
+								       NSEC_PER_MSEC),
+					   mt76_chip(&dev->mt76), mphy->band_idx,
+					   mphy->chandef.chan ? mphy->chandef.chan->center_freq : 0,
+					   mphy->dbg_scan_probe_reqs);
+		}
+	}
+
 	mt76_tx(mphy, control->sta, wcid, skb);
 }
 
Index: mt76-2025.11.06~eb567bc7/mt7915/mt7915.h
===================================================================
--- mt76-2025.11.06~eb567bc7.orig/mt7915/mt7915.h
+++ mt76-2025.11.06~eb567bc7/mt7915/mt7915.h
@@ -224,6 +224,17 @@ struct mt7915_phy {
 	struct mt76_mib_stats mib;
 	struct mt76_channel_state state_ts;
 
+	/* Debug timing/state */
+	u64 dbg_offchan_enter_ns;
+	u32 dbg_offchan_enter_freq;
+	bool dbg_last_offchannel;
+	u32 dbg_prog_freq;
+	u32 dbg_prog_center1;
+	u32 dbg_prog_center2;
+	u8 dbg_prog_width;
+	bool dbg_prog_offchan;
+	bool dbg_prog_valid;
+
 	bool sku_limit_en:1;
 	bool sku_path_en:1;
 
Index: mt76-2025.11.06~eb567bc7/mt7915/mcu.c
===================================================================
--- mt76-2025.11.06~eb567bc7.orig/mt7915/mcu.c
+++ mt76-2025.11.06~eb567bc7/mt7915/mcu.c
@@ -2787,6 +2787,7 @@ int mt7915_mcu_set_chan_info(struct mt79
 	struct cfg80211_chan_def *chandef = &phy->mt76->chandef;
 	int freq1 = chandef->center_freq1;
 	u8 band = phy->mt76->band_idx;
+	u64 t0_ns, t1_ns;
 	struct {
 		u8 control_ch;
 		u8 center_ch;
@@ -2845,7 +2846,32 @@ int mt7915_mcu_set_chan_info(struct mt79
 		req.center_ch2 = ieee80211_frequency_to_channel(freq2);
 	}
 
-	return mt76_mcu_send_msg(&dev->mt76, cmd, &req, sizeof(req), true);
+	if (cmd == MCU_EXT_CMD(CHANNEL_SWITCH)) {
+		t0_ns = ktime_get_boottime_ns();
+		printk(KERN_INFO
+		       "mt76: event=chan_switch_req ts_ms=%llu chip=0x%04x band=%u offchan=%d cmd=0x%x reason=%u ctrl=%u center=%u bw=%u freq1=%d\n",
+		       (unsigned long long)div_u64(t0_ns, NSEC_PER_MSEC),
+		       mt76_chip(&dev->mt76), band, phy->mt76->offchannel, cmd,
+		       req.switch_reason, req.control_ch, req.center_ch,
+		       req.bw, freq1);
+	}
+
+	{
+		int ret = mt76_mcu_send_msg(&dev->mt76, cmd, &req, sizeof(req), true);
+
+		if (cmd == MCU_EXT_CMD(CHANNEL_SWITCH)) {
+			t1_ns = ktime_get_boottime_ns();
+			printk(KERN_INFO
+			       "mt76: event=chan_switch_rsp ts_ms=%llu chip=0x%04x band=%u offchan=%d cmd=0x%x dur_ms=%llu ret=%d reason=%u ctrl=%u center=%u bw=%u freq1=%d\n",
+			       (unsigned long long)div_u64(t1_ns, NSEC_PER_MSEC),
+			       mt76_chip(&dev->mt76), band, phy->mt76->offchannel, cmd,
+			       (unsigned long long)div_u64(t1_ns - t0_ns, NSEC_PER_MSEC),
+			       ret, req.switch_reason, req.control_ch, req.center_ch,
+			       req.bw, freq1);
+		}
+
+		return ret;
+	}
 }
 
 static int mt7915_mcu_set_eeprom_flash(struct mt7915_dev *dev)
