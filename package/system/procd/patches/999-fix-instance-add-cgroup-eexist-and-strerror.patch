Index: procd-2026.02.09~7e5b324c/service/instance.c
===================================================================
--- procd-2026.02.09~7e5b324c.orig/service/instance.c
+++ procd-2026.02.09~7e5b324c/service/instance.c
@@ -28,6 +28,7 @@
 #include <unistd.h>
 #define SYSLOG_NAMES
 #include <syslog.h>
+#include <errno.h>
 
 #include <libubox/md5.h>
 
@@ -567,26 +568,30 @@ instance_add_cgroup(const char *service,
 	struct stat sb;
 	int fd, ret;
 
-	if (stat("/sys/fs/cgroup/cgroup.subtree_control", &sb))
-		return -ENOENT;
+	if (stat("/sys/fs/cgroup/cgroup.subtree_control", &sb)) {
+		if (errno == ENOENT)
+			return 0;
+		return -errno;
+	}
 
-	mkdir(CGROUP_BASEDIR, 0700);
+	if (mkdir(CGROUP_BASEDIR, 0700) == -1 && errno != EEXIST)
+		return -errno;
 
 	ret = snprintf(cgnamebuf, sizeof(cgnamebuf), "%s/%s", CGROUP_BASEDIR,
 		       service);
 	if (ret >= sizeof(cgnamebuf))
 		return -ENAMETOOLONG;
 
-	if (mkdir(cgnamebuf, 0700))
-		return -EPERM;
+	if (mkdir(cgnamebuf, 0700) == -1 && errno != EEXIST)
+		return -errno;
 
 	ret = snprintf(cgnamebuf, sizeof(cgnamebuf), "%s/%s/%s", CGROUP_BASEDIR,
 		       service, instance);
 	if (ret >= sizeof(cgnamebuf))
 		return -ENAMETOOLONG;
 
-	if (mkdir(cgnamebuf, 0700))
-		return -EPERM;
+	if (mkdir(cgnamebuf, 0700) == -1 && errno != EEXIST)
+		return -errno;
 
 	if (strlen(cgnamebuf) + strlen(cgroup_procs) >= sizeof(cgnamebuf))
 		return -ENAMETOOLONG;
@@ -595,7 +600,7 @@ instance_add_cgroup(const char *service,
 
 	fd = open(cgnamebuf, O_WRONLY);
 	if (fd == -1)
-		return -EIO;
+		return -errno;
 
 	dprintf(fd, "%d", getpid());
 	close(fd);
@@ -685,8 +690,8 @@ instance_start(struct service_instance *
 		closefd(epipe[0]);
 		ret = instance_add_cgroup(in->srv->name, in->name);
 		if (ret)
-			ULOG_WARN("failed adding instance cgroup for %s: %s\n",
-				  in->srv->name, strerror(ret));
+			ULOG_WARN("failed adding instance cgroup for %s/%s: %s\n",
+				  in->srv->name, in->name, strerror(-ret));
 
 		instance_run(in, opipe[1], epipe[1]);
 		return;
